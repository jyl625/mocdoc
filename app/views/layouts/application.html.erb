<!DOCTYPE html>
<html>
  <head>
    <meta property="og:title" content="MocDoc"/>
    <meta property="og:image" content="<%= image_url('mocdoc/app/assets/images/mocdoc_pic.png') %>"/>
    <meta property="og:description" content="A ZocDoc-inspired single-page web app. Helping patients find and book appointments with the right doctors based on their specialty and insurance coverage."/>
    <meta property="og:url" content="https://mocdoc.herokuapp.com/"/>
    <title>Mocdoc</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>

    <% if logged_in? %>
      <script type="text/javascript">
        window.currentUser = <%= render(
          "api/users/user.json.jbuilder",
          user: current_user
        ).html_safe %>
      </script>
    <% end %>

  </head>

  <body>
    <%= yield %>
  </body>
</html>
